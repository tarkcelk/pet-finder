import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  validationText: {
    fontSize: 16,
    color: 'red',
    width: '75%',
    alignSelf: 'center',
    textAlign: 'left',
  },
});
